---
type: tool
---

# Google Calendar

Always:

```text
python tools/run_tool/run_tool.py google_calendar commands
python tools/run_tool/run_tool.py google_calendar upcoming --days 14
```

Read JSON from stdout. Never write a scratch file. Spoken `MATH 212` is `--class "MATH 212"`. Never open `MATH 212.md`.
