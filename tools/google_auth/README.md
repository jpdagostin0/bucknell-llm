---
type: tool
---

# Google Auth

Always:

```text
python tools/run_tool/run_tool.py google_auth commands
python tools/run_tool/run_tool.py google_auth login
```

Read JSON from stdout. Never write a scratch file. Spoken `MATH 212` is `--class "MATH 212"`. Never open `MATH 212.md`.
