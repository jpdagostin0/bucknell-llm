---
type: tool
---

# Fast Fix Vault

Always:

```text
python tools/run_tool/run_tool.py fast_fix_vault commands
python tools/run_tool/run_tool.py fast_fix_vault
```

Read JSON from stdout. Never write a scratch file. Spoken `MATH 212` is `--class "MATH 212"`. Never open `MATH 212.md`.
