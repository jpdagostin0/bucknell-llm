---
type: tool
---

# Fast Weekly Review

Always:

```text
python tools/run_tool/run_tool.py fast_weekly_review commands
python tools/run_tool/run_tool.py fast_weekly_review --class "MATH 212"
```

Read JSON from stdout. Never write a scratch file. Spoken `MATH 212` is `--class "MATH 212"`. Never open `MATH 212.md`.
