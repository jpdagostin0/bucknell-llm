---
type: tool
---

# Fast Fetch Webpage

Always:

```text
python tools/run_tool/run_tool.py fast_fetch_webpage commands
python tools/run_tool/run_tool.py fast_fetch_webpage --url "https://example.com"
```

Read JSON from stdout. Never write a scratch file. Spoken `MATH 212` is `--class "MATH 212"`. Never open `MATH 212.md`.
