---
type: tool
---

# Fast Clean Vault

Runs the check-and-fix cycle once and reports whether the vault is clean or still needs reviewed repairs.

JSON in, JSON out. The default command is `run`. Items the runner will not guess are listed under `needs_llm`.

```powershell
.\tools\fast-clean-vault\fast-clean-vault.ps1
```
