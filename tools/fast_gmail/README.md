---
type: tool
---

# Fast Gmail

Always:

```text
python tools/run_tool/run_tool.py fast_gmail commands
python tools/run_tool/run_tool.py fast_gmail --query "is:unread newer_than:7d"
```

Read JSON from stdout. Never write a scratch file. Spoken `MATH 212` is `--class "MATH 212"`. Never open `MATH 212.md`.
